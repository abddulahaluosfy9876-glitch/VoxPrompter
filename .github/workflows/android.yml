name: Android CI-CD Autopilot

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  explore:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: List Repository Structure
      run: |
        echo "=== الأقسام الموجودة في المجلد الرئيسي ==="
        ls -R
        
