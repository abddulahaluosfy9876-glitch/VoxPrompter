<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:keepScreenOn="true"
    android:padding="20dp"
    android:layoutDirection="rtl">

    <LinearLayout
        android:id="@+id/statusLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_topToTopOf="parent">

        <TextView
            android:id="@+id/tvTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textSize="20sp"
            android:textColor="#FF3B30"
            android:textStyle="bold"
            android:layout_marginEnd="16dp"/>

        <ProgressBar
            android:id="@+id/readingProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="8dp"
            android:layout_weight="1"
            android:max="100"
            android:progressDrawable="@android:drawable/progress_horizontal"
            android:progressTint="#34C759"/>
    </LinearLayout>

    <ScrollView
        android:id="@+id/textScrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_verticalMargin="24dp"
        android:fillViewport="true"
        android:scrollbars="none"
        app:layout_topToBottomAt="@id/statusLayout"
        app:layout_bottomToTopOf="@id/controlLayout">

        <TextView
            android:id="@+id/tvTeleprompterText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="28sp"
            android:textColor="#FFFFFF"
            android:gravity="center"
            android:lineSpacingMultiplier="1.6"
            android:paddingVertical="100dp"
            android:text="هذا نص تجريبي ذكي للملقن الصوتي التلقائي. عندما تبدأ بالقراءة بصوتك الواضح، سيقوم التطبيق بالاستماع إليك ومطابقة كلماتك مع هذا النص، ثم تحريك الشاشة تلقائياً لتبقيك دائماً في المنتصف دون الحاجة للمس الهاتف." />
    </ScrollView>

    <LinearLayout
        android:id="@+id/controlLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_bottomtoBottomOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnStart"
            android:layout_width="200dp"
            android:layout_height="56dp"
            android:text="ابدأ القراءة والتسجيل"
            android:textSize="18sp"
            android:textStyle="bold"
            app:backgroundTint="#007AFF"
            app:cornerRadius="28dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnStop"
            android:layout_width="200dp"
            android:layout_height="56dp"
            android:text="إنهاء وحفظ"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:backgroundTint="#FF3B30"
            app:cornerRadius="28dp"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
      
